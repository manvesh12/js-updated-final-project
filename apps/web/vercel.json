{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && npm install --no-audit --no-fund --legacy-peer-deps && npm run build --workspace @dsr/web",
  "outputDirectory": ".next",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://YOUR_AWS_API_DOMAIN/api/:path*"
    }
  ]
}
